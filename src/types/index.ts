export type PostImage = {
  date?: string;
  explanation?: string;
  hdurl?: string;
  media_type?: string;
  title?: string;
  url?: string;
};

export type RootStackParams = {
  Detail: PostImage;
};
